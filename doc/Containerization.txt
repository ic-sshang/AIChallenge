Here is a complete example of a 
.NET 8 minimal API
 project, containerized with a 
Dockerfile
. It includes:
A simple 
Program.cs
 with a greeting endpoint.
A 
Dockerfile
 that builds and runs the app using official .NET 8 SDK and runtime images.
🧱 Project Highlights
Program.cs
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapGet("/", () => "Hello from a containerized .NET 8 minimal API!");

app.Run();

Dockerfile
# Build stage
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src
COPY . .
RUN dotnet publish -c Release -o /app

# Runtime stage
FROM mcr.microsoft.com/dotnet/aspnet:8.0
WORKDIR /app
COPY --from=build /app .
EXPOSE 8080
ENTRYPOINT ["dotnet", "MinimalApi.dll"]

▶️ To Build and Run
# Unzip the project
unzip dotnet_minimal_api_project.zip
cd dotnet_minimal_api

# Build the Docker image
docker build -t dotnet-minimal-api .

# Run the container
docker run -p 8080:8080 dotnet-minimal-api

Then visit 
http://localhost:8080
 in your browser.
 URL:/spaces/PMK/pages/4153442312/Containerization