Common Queries:
Database Servers:
Need to do mssql training  - 
https://invoicecloud.mindtickle.com/new/ui/learner#/update/1498398091599651520?series=1447562493769400856
ic-dbtest-east-server1.database.windows.net
,1433 (Biller ShardsServer) 
ic-gateway-east.database.windows.net
,1433 (Gateway Servers)
Deployment of console and web app in testing and prod environment end point.
Answer: Applications have a deployment pipeline which promotes the build artifact to the various environments.  These YAML pipelines are part of the code repo.
Do we have any host setting for our project Console or web api projects? Any initial configuration or setup changes is require for project setup like Certificate installation PKI, environment variable value setup.
Answer: It will depend on the project.  There are test database servers and test accounts for some external systems.
Any License key for Visual studio. 
Need to login with your invoice cloud credential it will take License .
Environment variable setup as project wise in solution.
where we Logging Exception or error in console or batch jobs in local (Any Path).
Answer: We use a serillog library and log to New Relic.  This is for both WWW and jobs.  When we move to Phase 2 (new .Net 6 and containerized applications) we will log to the STDOUT and STDERR streams.
Can we have a log access of all environment (any one team member) or share the contact person so we can connect and get the logs accordingly? 
Anser: Yes.  You can get access to the logs either by coordination with IC tech lead or by getting an account in New Relic (these are limited so it might be via coordination).
Database stored procedure change as per the billerdb or any common place need to change. Can developer do this changes?
Answer: Yes and no.  We have DBAs who have their own SDLC requirements for database assets and unless they are unavailable would prefer to do the work.
how to get authorization token (any api reference with require input header).
SubscriberID
API Key
Database stored procedure.
Answer: The test environment IC subscriber API key is a constant and can be provided to you for testing. 
For alert and email notification ? end point , dashboard, codebase etc.
if we have new changes in to database with new field or any parameter do we need to validate with alert msg and notification ?
Any test case do we need to perform by testing team ?
Logging End points.
Code Versioning
Git
PR Raise as per story and task with new branch.
PR (Pull Request) should be Build, Unit Test covered.
Address the all review and comments via the team (2 acceptance is require).
Then PR will merge.
Database changes specific biller shard database or all the billershard database.
What is the procedure we need to follow when changes require in db.
Answer: There will be a DB team member in the scrum team.  You will create a DB subtask for a given software Jira ticket requesting the DB work required.
Base Address of the RMM API Url ? perform testing purpose. 
Answer: Each team has a test web server which will determine the base address, example https://devtest2.invoicecloud.com
User Story: 
EBPP-6017
2278e126-27dc-36c8-aef9-a32d91bc7ad7
System JIRA
 
Queries:
N/A Common (Have not debug the code and check the stored procedure due to access issue).
User Story:
 
EBPP-5835
2278e126-27dc-36c8-aef9-a32d91bc7ad7
System JIRA
Queries:
Base Address of the RMM API Url ? perform testing purpose?
Can we use sample request for RMM API
{ "MerchantAccountID":"biller1", "ListOfAchRejectIDs": ["2","3"], "AccountNumber":"1000000000", "RoutingNumber":"011000015", "AccountType":"CHECKING" }
 URL:/spaces/EN/pages/2537521206/Technical+Queries