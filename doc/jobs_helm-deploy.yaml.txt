Code-Generated Docs
Description

This template runs the template `tasks/helm-deploy.yaml` against a single environment
The actual helm deployment commands are in the task template; this one calculates all the values and provides them to that template. 

Parameters
Parameter
Default
Description
deploymentName
n/a
name of the deployment
instance
n/a
name of the instance to target for deployment
spoke
n/a
spoke name. used to build Azure and Kubernetes resource names as well as determine which variable groups to access
serviceName
n/a
service name. used only to build Azure and Kubernetes resource names
chartName
n/a
name of the chart being deployed
infrastructureVersion
n/a
spoke infrastructure version. used to build Azure resource names
environment
n/a
environment being targeted. used to build Azure resource names, determine variable groups to access, and passed as a value to the helm deploy command
resourceNameOverrides
[]
dictionary of name patterns which will override the defaults in this file
chartVersion
n/a
version of the chart to deploy
imageTag
n/a
tag of the image the chart will deploy
testFilterExtension
" "
testFilterExtension for integration/smoke tests running in deployment pipeline
testFilter
" "
testFilterExtension for integration/smoke tests running in deployment pipeline
dependsOn
[]
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
flows/helm-deploy.yaml
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
../tasks/helm-deploy.yaml
../tasks/override-variables.yaml
../jobs/dotnet-framework/post-helm-deploy-test.yaml
../tasks/get-mi-client-id.yaml
 URL:/spaces/platform/pages/3634593930/jobs+helm-deploy.yaml