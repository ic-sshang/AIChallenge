Code-Generated Docs
Description
None :(
Parameters
Parameter
Default
Description
artifact
n/a
version
n/a
managedIdentityAppName
" "
serverType
" "
environments
n/a
Per every stage defined
  - ${{ each env in parameters.environments
targetDir
n/a
projectName
n/a
preDeploySteps
[]
postDeploySteps
[]
preDeployJob
[]
Pre-Deploy Job
          - ${{ if ne(length(parameters.preDeployJob
postDeployJob
[]
Post-Deploy Job
          - ${{ if ne(length(parameters.postDeployJob
IsSkybotJob
False
kongRouteDomain
" "
Kong Routes Deploy
          - ${{ if and(gt(length(parameters.kongRouteDomain
kongIncomingPathPrefix
" "
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
icgateway-deploy.yaml
console-app-deploy.yaml
proxy-deploy.yaml
web-deploy.yaml
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
../jobs/test.yaml
../jobs/deploy-kong-routes.yaml
../jobs/deploy.yaml
 URL:/spaces/platform/pages/3635150948/flows+deployment-multi-job.yaml