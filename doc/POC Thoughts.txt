This document lists functionality to prove out in a POC stage
Can add domain over Mailgun API
Can get DNS requirements for that added domain via Mailgun API
Can validate DNS status via Mailgun API or some 3rd party service
Can pull the default 
smtp
 credential via Mailgun API after domain creation
Can send email through that Biller Domain after set up
FROM in received email looks accurately sent from that domain
At min, it should not have the on-behalf-of string in the from
Marked email as spam event is delivered back to Mailgun and specifically the Biller Domain
Marked email as do not email / block is delivered back to Mailgun and specifically the Biller Domain
 URL:/spaces/EA/pages/3924459532/POC+Thoughts