Multi-Customer Login 
Customer Registration ❌ 
Optionally, filter out accounts that don’t exist 
Filter out rows where EmailAddress/Username or Password are empty 
Filter out accounts that have more than 1 owner (LoginType 1) 
Filter out logins that already exist 
Write errors to distinct files 
Write valid lines to a new file 
AutoPay ❌ 
Filter out accounts that occur more than once 
Filter out accounts that are already enrolled 
Filter out accounts that do not exist 
Filter out rows where the EmailAddress/Username is empty 
Filter out accounts where the login doesn’t exist 
Filter out accounts where the login doesn’t have a password 
Remove rows with invalid payment methods 
RoutingNumber length != 9 
BankAccountNumber length < 4 
BankAccountNumber/RoutingNumber/CreditCardNumber contain + or E 
Expired CC 
Write errors to distinct files 
Write valid lines to a new file 
Paperless ❌ 
Filter out accounts that occur more than once 
Filter out accounts that are already enrolled 
Remove accounts that don’t exist 
Filter out accounts where the login doesn’t exist 
Remove rows with multiple emails (look for semicolon, comma, or space) 
Write errors to distinct files 
Write valid lines to a new file 
Payment Methods ❌ 
Remove accounts that don’t exist 
Filter out accounts where the login doesn’t exist 
Filter out accounts where the login doesn’t have a password 
Remove rows with invalid payment methods 
RoutingNumber length != 9 
BankAccountNumber length < 4 
BankAccountNumber/RoutingNumber/CreditCardNumber contain + or E 
Expired CC 
Write errors to distinct files 
Write valid lines to a new file 
Non-MCL 
AutoPay 🔵 
Filter out accounts that occur more than once ✅ 
Optionally, keep the first occurrence ✅ 
Optionally, keep one occurrence if the rows are identical ❌ 
Filter out accounts that are already enrolled ✅ 
Optionally, remove accounts that don’t exist ✅ 
Remove them if the file does not have billing information ❌ 
Remove rows with invalid payment methods 
RoutingNumber length != 9 ✅ 
BankAccountNumber length < 4 ✅ 
BankAccountNumber/RoutingNumber/CreditCardNumber contain + or E❌ 
Expired CC ❌ 
Write errors to distinct files ✅ 
Write valid lines to a new file ✅ 
Paperless 🔵 
Filter out accounts that occur more than once ✅ 
Optionally, keep the first occurrence ✅ 
Optionally, keep one occurrence if the rows are identical ❌ 
Filter out accounts that are already enrolled ✅ 
Optionally, remove accounts that don’t exist ✅ 
Remove rows with multiple emails (look for semicolon, comma, or space) ❌ 
Remove rows with empty emails ❌ 
Write errors to distinct files ✅ 
Write valid lines to a new file ✅ 
Payment Methods ❌ 
Optionally, remove accounts that don’t exist 
Remove them if the file does not have billing information 
Remove rows with invalid payment methods 
RoutingNumber length != 9 
BankAccountNumber length < 4 
BankAccountNumber/RoutingNumber/CreditCardNumber contain + or E 
Expired CC 
Write errors to distinct files 
Write valid lines to a new file 
 URL:/spaces/PE/pages/4556587077/BIF-196+Data+Conversion+Preprocessing