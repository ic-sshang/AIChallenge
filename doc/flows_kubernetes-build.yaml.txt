Code-Generated Docs
Description

builds all containers and helm charts in a repo and publishes all to an ACR with the same tag

Parameters
Parameter
Default
Description
containers
[]
list of container objects. container object must have property `name`. container object may have properies `os`, `extraBuildArgs`, and/or `dockerfile`
charts
[]
list of chart objects. chart object must have `name`. no other properties are supported
spokeName
" "
Spoke where the app runs. Only used to access the correct variable groups.
glbVersion
1
Current version of global infrastructure resources. Should usually be updated in the template, not updated in a pipeline's YAML
dependsOn
[]
used for control flow of pipeline to ensure that these jobs run after others if provided
gitVersionConfigFile
GitVersion.yml
path to the GitVersion config file
variableGroups
[]
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
../jobs/docker-build.yaml
../jobs/helm-build.yaml
../tasks/calculate-semver.yaml
 URL:/spaces/platform/pages/3634659466/flows+kubernetes-build.yaml