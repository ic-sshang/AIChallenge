summary
Machine Configuration
 is 
an abstract object schema
 and the only object schema under the Machine Configurations top-level schema. All CI’s related to server configurations will be some child type under this schema.
All child object types of this schema
 inherit its attributes
.
Attributes
Currently, there is only one custom attribute for the Machine Configuration schema: 
Virtual Machines
. The 
Virtual Machines
 attribute is a field containing a list of VM’s referenced with 
is configured on
 URL:/spaces/platform/pages/4213375051/Schema+Machine+Configuration