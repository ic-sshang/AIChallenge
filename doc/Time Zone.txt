Our servers run in CST regardless of where they were deployed. The time zone for a Windows server is scrubbed when SysPrep runs, so must be configured after deployment.
Post-Provisioning
Our servers run in CST regardless of where they were deployed. The time zone for a Windows server is scrubbed when SysPrep runs, so must be configured after deployment.
 URL:/spaces/platform/pages/3946020946/Time+Zone