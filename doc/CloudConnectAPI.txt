CloudConnectAPI 
is a class library (dll) whose methods can be called to trigger a CloudConnect popup window to make payments; billers use a 3rd party application to reference it & call the sale/credit method(s), e.g. Cayenta / ETIMS
 URL:/spaces/ED/pages/3742040066/CloudConnectAPI