Code-Generated Docs
Description

This template lists all images (and their versions) referenced by a helm chart.
It is used in the image promotion flow.
Once the images are listed, another job takes the task of promoting them.

Parameters
Parameter
Default
Description
acr
n/a
ACR containing the helm chart to scan
chartName
n/a
Name of the chart to scan
chartVersion
n/a
Version of the chart to scan
version
n/a
Value to be passed as image.tag
valuesFile
n/a
Relative path to config file containing values for chart
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
jobs/list-images-to-promote.yaml
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
 URL:/spaces/platform/pages/3634659485/tasks+get-images-in-chart.yaml