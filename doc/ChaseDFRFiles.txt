ChaseDFRFiles 
Unzip raw file and split file into respective directories
Purpose:
The raw zip files are unzipped and moved to their respective directories and then parses the file. 
Context:
This job is important since it splits the file and parses to populate the database.
How To Check
Check if the files are downloaded from blob storage
Make sure the directories are in place
Check if files are unzipped and split into the respective directories
https://invoicecloud.visualstudio.com/Src/_git/ChaseDFRFiles?path=/ChaseDFRFiles/Module1.vb
Questions to Answer
Did the files download to their respective directories?
OpsGenie Alert
 URL:/spaces/DS/pages/2522415105/ChaseDFRFiles