Install Cloudflare WARP Client
Note: For InvoiceCloud employees this will be provisioned by the EngageSmart desktop team automatically.
Open the WARP Client
Click the Cog icon and select Preferences > Account
Login to Zero Trust
Enter engagesmart for team name
Authenticate through Okta
Toggle WARP Client to Connected
Click the cog and select virtual networks > InvoiceCloud-EastUs-Non-Prod
Note: This will expand to other regions in the future, following the same naming pattern InvoiceCloud-${Location}-${Environment}
To switch between environments right click on Warp Client and select virtual network based on environment you need to connect to.
 
 
Navigate to: 
https://client.wvd.microsoft.com/arm/webclient/v2/index.html
Select SessionHost
Connect and authenticate if required
Can either open connection in browser and Use the single monitor
or download RDP Program using this 
link
To open using this method
 
Once Connected Most programs needed are required and will install automatically.
Machine needs to be logged into and turned on for programs to install and may take 2-3 hours to complete all of them. 
 URL:/spaces/icinfradoc/pages/2808053779/FTE+AVD+Connection+Workflow