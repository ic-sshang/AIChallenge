Code-Generated Docs
Description
None :(
Parameters
Parameter
Default
Description
artifact
n/a
name of the artifact to deploy
version
n/a
version of the artifact to deploy
managedIdentityAppName
" "
MI name for the app. Providing this will prompt the pipeline to pull the client ID at deployment time
environments
n/a
Object of environment configurations. Each environment should contain 'name', 'miEnvironment', 'tests', and 'variableGroups'. Devtest can also pass 'tags'
targetDir
n/a
target directory for the artifact to be deployed to on the web server
projectName
n/a
name of the repo/project. Only used to run Sonar scan on test code. Should probably be removed...
preDeploySteps
[]
Steps to run on the web server before the deployment occurs
postDeploySteps
[]
Steps to run on the web server after the deployment occurs
preDeployJob
[]
Steps to run on a pipeline agent before the deployment job (runs before preDeploySteps)
postDeployJob
[]
Steps to run on a pipeline agent after the deployment job (runs after postDeploySteps)
IsSkybotJob
False
Actually has nothing to do with Skybot. Determines whether artifact is in root of zip file, or /build in zip file. Ideally, we'd standardize to root and deprecate down the road.
iisBindings
n/a
IIS Bindings to update
iisSitePath
n/a
IIS site under which the app is running
iisAppPoolName
n/a
IIS App Pool Name
manual_override_stages
False
Deprecated. Do not use. Will be removed in a future release.
kongRouteDomain
Kong Route Domain
kongIncomingPathPrefix
" "
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
tasks/server-artifact-cleanup.yaml@pipeline-templates
flows/deployment-multi-job.yaml
jobs/configuration-file-encryption.yaml@pipeline-templates
tasks/update-iis-bindings.yaml
 URL:/spaces/platform/pages/3634954458/web-deploy.yaml