CloudConnect 
is a WPF desktop application billers use to make payments that runs in the background (has an icon in the system tray) until it's called by CloudConnectAPI.  It is deployed as a standalone app with its own installer which also adds the CloudConnectAPI dlls in the Program Files
 URL:/spaces/ED/pages/3742203905/CloudConnect