Code-Generated Docs
Description

runs 'helm upgrade --install' to deploy a helm chart to a cluster

Parameters
Parameter
Default
Description
deploymentName
n/a
name of the deployment in kubernetes
cluster
n/a
name of the cluster targeted by this deployment
spokeResourceGroup
n/a
name of the resource group containing the cluster
chartName
n/a
name of the chart to deploy
chartVersion
n/a
version of the chart to deploy
imageTag
n/a
tag of the image the chart will deploy
instance
n/a
instance name. provided directly as a value to chart with `--set instance=`
environment
n/a
environment name. provided directly as a value to chart with `--set environment=` also used to determine environment-specific values file to provide to chart
clientId
n/a
client ID of identity under which workloads deployed by chart should run. provided directly as a value to the chart with `--set clientID=`
namespace
n/a
namespace targeted by deployment
acr
acrglbimages1sbx.azurecr.io
acr containing the helm chart to deploy
environmentsDirectory
.helm/environments
directory containing {environment}.yaml
subscription
n/a
subscription name/ID containing target cluster
Template Dependencies
These lists show which templates are consumed by this one, and which templates consume it.
Used By
Any templates listed here are calling this one. If there are none, no other templates are dependent on this one.
jobs/helm-deploy.yaml
Uses
Any templates listed here are called by this one. If there are none, this template is not dependent on any others.
 URL:/spaces/platform/pages/3633480311/tasks+helm-deploy.yaml